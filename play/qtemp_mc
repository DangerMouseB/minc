#!/bin/sh

DIR=`cd $(dirname $0); pwd`
SRC_HOME=$DIR

rm -f $SRC_HOME/temp_mc
rm -f $SRC_HOME/temp_mc.o
rm -f $SRC_HOME/temp_mc.s

../qq $SRC_HOME/temp_mc.q
cc $SRC_HOME/temp_mc.o -o $SRC_HOME/temp_mc -lc
./queen2_mc
